<% form_for(@group, :url => project_groups_path(@project)) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.hidden_field :project_id %>
    <%= f.submit t(submit_label(@group)) %>
  </p>
<% end %>
